import { ArrowLeft, Check, ChevronRight, Loader2, UserRound } from 'lucide-react';
import { useState } from 'react';
import { Link, useLocation } from 'wouter';
import { useCreateProvider, getGetDashboardSummaryQueryKey, getListProvidersQueryKey } from '@workspace/api-client-react';
import { useQueryClient } from '@tanstack/react-query';
import { CameraCapture } from '@/components/camera-capture';
import { useToast } from '@/hooks/use-toast';

export default function Cadastro() {
  const [, setLocation] = useLocation(); const queryClient = useQueryClient(); const { toast } = useToast(); const createProvider = useCreateProvider();
  const [form, setForm] = useState({ name: '', rg: '', company: '', service: '' }); const [photo, setPhoto] = useState<string | null>(null); const [done, setDone] = useState(false);
  const update = (key: keyof typeof form) => (event: React.ChangeEvent<HTMLInputElement>) => setForm((current) => ({ ...current, [key]: event.target.value }));
  const valid = Object.values(form).every((value) => value.trim().length >= 2) && form.rg.trim().length >= 3;
  function submit(event: React.FormEvent) { event.preventDefault(); if (!valid) return; createProvider.mutate({ data: { name: form.name.trim(), rg: form.rg.trim(), company: form.company.trim(), defaultService: form.service.trim(), photoData: photo } }, { onSuccess: (provider) => { setDone(true); queryClient.invalidateQueries({ queryKey: getListProvidersQueryKey() }); queryClient.invalidateQueries({ queryKey: getGetDashboardSummaryQueryKey() }); toast({ title: 'Cadastro concluído', description: `${provider.name} agora pode entrar rapidamente.` }); setTimeout(() => setLocation('/'), 1300); } }); }
  if (done) return <div className="flex min-h-[70vh] items-center justify-center"><div className="rise-in max-w-sm text-center"><div className="mx-auto mb-5 grid h-16 w-16 place-items-center rounded-2xl bg-primary text-primary-foreground"><Check className="h-8 w-8" /></div><h1 className="text-2xl font-bold tracking-tight">Prestador cadastrado</h1><p className="mt-2 text-sm text-muted-foreground">A identidade de {form.name} foi salva. Voltando à visão geral...</p></div></div>;
  return <div className="mx-auto max-w-5xl space-y-7"><div className="rise-in flex items-center gap-4"><Link href="/" data-testid="link-back-dashboard" className="grid h-9 w-9 place-items-center rounded-lg border border-border bg-card text-muted-foreground transition-colors hover:text-foreground"><ArrowLeft className="h-4 w-4" /></Link><div><div className="mb-1 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.18em] text-primary">Novo registro <ChevronRight className="h-3 w-3" /> Etapa única</div><h1 className="text-3xl font-bold tracking-[-.04em]">Cadastrar prestador</h1></div></div>
    <div className="grid gap-5 lg:grid-cols-[1fr_340px]"><form onSubmit={submit} className="rise-in delay-1 rounded-2xl border border-border bg-card p-5 sm:p-7"><div className="mb-7 flex items-start gap-3 border-b border-border pb-6"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-primary/10 text-primary"><UserRound className="h-5 w-5" /></div><div><h2 className="font-bold">Dados de identificação</h2><p className="mt-1 text-xs text-muted-foreground">Esses dados não serão solicitados novamente nas próximas visitas.</p></div></div><div className="grid gap-5 sm:grid-cols-2"><label className="sm:col-span-2"><span className="mb-2 block text-xs font-semibold">Nome completo <i className="text-accent-foreground">*</i></span><input value={form.name} onChange={update('name')} data-testid="input-provider-name" placeholder="Ex.: Carlos Eduardo Lima" className="h-11 w-full rounded-xl border border-input bg-background px-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/10" /></label><label><span className="mb-2 block text-xs font-semibold">RG <i className="text-accent-foreground">*</i></span><input value={form.rg} onChange={update('rg')} data-testid="input-provider-rg" placeholder="00.000.000-0" className="h-11 w-full rounded-xl border border-input bg-background px-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/10" /></label><label><span className="mb-2 block text-xs font-semibold">Empresa <i className="text-accent-foreground">*</i></span><input value={form.company} onChange={update('company')} data-testid="input-provider-company" placeholder="Nome da empresa" className="h-11 w-full rounded-xl border border-input bg-background px-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/10" /></label><label className="sm:col-span-2"><span className="mb-2 block text-xs font-semibold">Serviço padrão <i className="text-accent-foreground">*</i></span><input value={form.service} onChange={update('service')} data-testid="input-provider-service" placeholder="Ex.: Manutenção de ar-condicionado" className="h-11 w-full rounded-xl border border-input bg-background px-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/10" /></label></div><div className="mt-8 flex flex-col-reverse justify-between gap-3 border-t border-border pt-5 sm:flex-row sm:items-center"><p className="text-[11px] text-muted-foreground"><i className="text-accent-foreground">*</i> Campos obrigatórios</p><div className="flex gap-2"><Link href="/" data-testid="link-cancel-cadastro" className="inline-flex h-10 items-center justify-center rounded-lg px-4 text-xs font-semibold text-muted-foreground hover:text-foreground">Cancelar</Link><button type="submit" disabled={!valid || createProvider.isPending} data-testid="button-submit-provider" className="inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-primary px-5 text-xs font-semibold text-primary-foreground transition-transform hover:-translate-y-0.5 disabled:pointer-events-none disabled:opacity-50">{createProvider.isPending && <Loader2 className="h-4 w-4 animate-spin" />}Salvar cadastro</button></div></div>{createProvider.isError && <p className="mt-3 text-right text-xs text-destructive">Não foi possível salvar. Confira os dados e tente novamente.</p>}</form>
      <div className="rise-in delay-2 rounded-2xl border border-border bg-card p-5 sm:p-6"><div className="mb-5"><h2 className="font-bold">Retrato</h2><p className="mt-1 text-xs leading-relaxed text-muted-foreground">Uma foto torna o reconhecimento na chegada mais ágil e seguro.</p></div><CameraCapture value={photo} onChange={setPhoto} /></div>
    </div>
  </div>;
}